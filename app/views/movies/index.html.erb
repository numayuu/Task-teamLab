 <div>
  <table>
    <h2>メモ一覧</h2>
      <th>タイトル</th>
      <th>評価</th>
      <% @movies.each do |movie| %>
        <tr>
          <td><%= link_to movie.title, movie_path(movie.id) %></td>
          <td><%= movie.body %></td>
        </tr>
      <% end %>
  </table>
</div>